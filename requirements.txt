# For GPU training, install a CUDA build matching your driver instead, e.g.:
#   pip install --index-url https://download.pytorch.org/whl/cu130 torch torchvision
# (see README) rather than relying on the CPU-only build these two lines resolve to.
torch
torchvision
rawpy
opencv-python
albumentations
pandas
scikit-learn
timm>=1.0.20
matplotlib
pyarrow
tqdm
PyYAML

# EyePose-v0 (an optional Classic Vision eye-detector backend - see
# picklikeme.eyes.eyepose_v0's module docstring) needs onnxruntime. Not
# listed unconditionally above since CPU vs GPU is a real choice, not a
# default - pick exactly one:
#   pip install onnxruntime               # any machine (CPU)
#   pip install onnxruntime-gpu==1.28.0    # NVIDIA/CUDA machines - matches the cu130 torch build above
# (same two choices as pyproject.toml's eyepose-cpu/eyepose-gpu extras)
