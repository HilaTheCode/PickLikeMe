# For GPU training, install a CUDA build matching your driver instead, e.g.:
#   pip install --index-url https://download.pytorch.org/whl/cu130 torch torchvision
# (see README) rather than relying on the CPU-only build these two lines resolve to.
torch
torchvision
rawpy
opencv-python
albumentations
pandas
scikit-learn
timm>=1.0.20
matplotlib
pyarrow
tqdm
